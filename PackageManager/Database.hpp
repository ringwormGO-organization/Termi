#pragma once

/* include */
#include <iostream>
#include <map>

/* commands */
std::string test_command;

/* database */
std::map<std::string, std::string> database = 
{
    {"test", test_command,},
};
